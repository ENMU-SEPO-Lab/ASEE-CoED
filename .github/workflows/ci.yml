name: Java AutoGrader CI (Gradle Optimized)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  grade:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      # This action handles caching of Gradle dependencies automatically
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # Run Compilation, Tests, PMD, and Checkstyle
      # --no-daemon saves memory on CI runners
      - name: Build and Check
        run: ./gradlew build pmdMain checkstyleMain --no-daemon

      # Run your custom grading script
      - name: Run grading script
        if: always()
        run: |
          if [ -f grade.py ]; then
            python grade.py
          fi

      # Upload Reports (Gradle puts them in build/reports)
      - name: Upload grading feedback
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: grading-reports
          path: build/reports/
          retention-days: 5

      # Email Feedback to Student
      - name: Send Feedback Email
        if: always()
        env:
          GMAIL_USER: alarusie@gmail.com
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: |
          python <<'EOF'
          import os, re, smtplib
          from email.message import EmailMessage
          from pathlib import Path

          student_email = "essa.imhmed@enmu.edu" # Fallback
          for java_file in Path('upload_here').rglob('*.java'):
              with open(java_file, 'r') as f:
                  match = re.search(r'@author\s+([^\s]+)', f.read())
                  if match:
                      student_email = f"{match.group(1)}@enmu.edu"
                      break

          msg = EmailMessage()
          msg['Subject'] = 'Automated Grading Feedback'
          msg['From'] = os.environ['GMAIL_USER']
          msg['To'] = student_email
          msg.set_content("Hello,\n\nYour automated grading feedback is attached.\n\nRegards,\nDr. Imhmed")

          # Gradle places XML/HTML reports in build/reports/
          feedback = Path("build/reports/feedback.md") 
          if not feedback.exists():
              feedback = Path("reports/feedback.md")

          if feedback.exists():
              with open(feedback,'rb') as f:
                  msg.add_attachment(f.read(), maintype='text', subtype='plain', filename='feedback.md')

          try:
              with smtplib.SMTP('smtp.gmail.com', 587) as server:
                  server.starttls()
                  server.login(os.environ['GMAIL_USER'], os.environ['GMAIL_APP_PASSWORD'])
                  server.send_message(msg)
              print(f"Email sent to {student_email}")
          except Exception as e:
              print(f"Email failed: {e}")
          EOF
